<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Payment Failed</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link href="/theme.css" rel="stylesheet">
  <style>
    body {
      min-height: 100vh;
      background-size: cover;
      background-position: center center;
      background-repeat: no-repeat;
      background-attachment: fixed;
      background-color: #111213;
      color: #fff;
      font-family: 'Montserrat', system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    body::before {
      content: '';
      position: fixed;
      inset: 0;
      background: rgba(0,0,0,0.45);
      pointer-events: none;
      z-index: -1;
    }
    .fail-card {
      background: rgba(255,255,255,0.05);
      border: 1px solid rgba(255,255,255,0.08);
      border-radius: 22px;
      padding: 45px 40px;
      width: 100%;
      max-width: 460px;
      text-align: center;
      box-shadow: 0 20px 50px rgba(0,0,0,0.55);
      -webkit-backdrop-filter: blur(12px);
      backdrop-filter: blur(12px);
    }
    .fail-icon {
      width: 120px;
      height: 120px;
      margin: 0 auto 20px;
      border-radius: 50%;
      background: radial-gradient(circle at top, rgba(255,107,107,0.9), rgba(220,38,38,0.95));
      display: flex;
      align-items: center;
      justify-content: center;
      box-shadow: 0 0 35px rgba(220, 38, 38, 0.5);
    }
    .fail-icon img { width: 55px; }
    .fail-title {
      font-size: 1.45rem;
      font-weight: 700;
      margin-bottom: 10px;
      color: #fff;
    }
    .fail-message {
      font-size: 0.95rem;
      color: rgba(255,255,255,0.7);
      margin-bottom: 28px;
    }
    .fail-actions {
      display: flex;
      gap: 12px;
      justify-content: center;
      flex-wrap: wrap;
    }
    .retry-btn {
      padding: 10px 26px;
      border-radius: 999px;
      border: 1px solid rgba(255,255,255,0.35);
      background: rgba(255,255,255,0.08);
      color: #fff;
      text-decoration: none;
      font-size: 0.9rem;
      font-weight: 600;
    }
    .retry-btn:hover { background: rgba(255,255,255,0.16); color: #fff; }
    .fail-note {
      margin-top: 22px;
      font-size: 0.8rem;
      color: rgba(255,255,255,0.6);
    }
  </style>
</head>
<body>
  <div class="fail-card">
    <div class="fail-icon">
      <img src="/images/redCross.png" alt="Transaction Failed">
    </div>
    <div class="fail-title"><%= message %></div>
    <div class="fail-message">
      Your payment was not completed. No charges were made.
      Please try again or use a different payment method.
    </div>
    <div class="fail-actions">
      <a href="/checkout" class="retry-btn">Try Again</a>
    </div>
    <div class="fail-note" id="redirect-msg">
      Redirecting back to checkout in <strong>4</strong> seconds...
    </div>
  </div>
  <script>
    let seconds = 4;
    const msg = document.getElementById('redirect-msg');
    const countdown = setInterval(() => {
      seconds--;
      if (msg) {
        msg.innerHTML = `Redirecting back to checkout in <strong>${seconds}</strong> seconds...`;
      }
      if (seconds <= 0) {
        clearInterval(countdown);
        window.location.href = '/checkout';
      }
    }, 1000);
  </script>
</body>
</html>
