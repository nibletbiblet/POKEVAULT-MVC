<!--
 I declare that this code was written by me. 
 I will not copy or allow others to copy my code. 
 I understand that copying code is considered as plagiarism.
 
 Student Name: NGJINHENG 
 Student ID: 24024323 
 Class: C372-003-E63C
 Date created: 1/2/2026
  -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link href="/theme.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>Request Refund</title>
  <style>
    body {
      background:#111213;
      color:#fff;
      font-family:'Montserrat',system-ui,-apple-system,'Segoe UI',Roboto,'Helvetica Neue',Arial,sans-serif;
      background-size:cover;
      background-repeat:no-repeat;
      background-attachment:fixed;
    }
    body::before { content:''; position:fixed; inset:0; background:rgba(0,0,0,0.45); z-index:-1; }
    .custom-navbar { background-color:transparent !important; padding-top:1.05rem; padding-bottom:1.05rem; min-height:80px; z-index:1030; box-shadow:none; }
    .custom-navbar .navbar-brand { font-weight:700; letter-spacing:-0.01em; }
    .brand-logo { font-family:'Montserrat',system-ui,sans-serif; display:inline-flex; flex-direction:column; text-align:center; text-decoration:none; line-height:1.05; padding-left:0.1rem; }
    .brand-main { font-size:1.45rem; font-weight:700; letter-spacing:0.33em; text-transform:uppercase; color:#ffffff; }
    .brand-sub { font-size:0.68rem; font-weight:500; letter-spacing:0.28em; text-transform:uppercase; color:rgba(255,255,255,0.75); margin-top:0.15rem; }
    .card-frosted { background: rgba(255,255,255,0.05); border: 1px solid rgba(255,255,255,0.08); border-radius: 0.8rem; -webkit-backdrop-filter: blur(12px); backdrop-filter: blur(12px); box-shadow: 0 12px 34px rgba(0,0,0,0.45); padding: 1.2rem; }
    .item-thumb { width: 40px; height: 40px; object-fit: cover; border-radius: 0.45rem; border: 1px solid rgba(255,255,255,0.12); box-shadow: 0 6px 16px rgba(0,0,0,0.35); }
    .input-ghost { background: rgba(255,255,255,0.08); border: 1px solid rgba(255,255,255,0.12); color: #fff; }
    .input-ghost:focus { background: rgba(255,255,255,0.12); color: #fff; border-color: rgba(255,255,255,0.32); box-shadow: 0 0 0 0.2rem rgba(255,255,255,0.12); }
    .input-ghost::placeholder { color: rgba(255,255,255,0.55); }
    .input-ghost option { background-color: #2b354a; color: #ffffff; }
    .input-ghost option:checked { background-color: #3c4b66; color: #ffffff; }
    /* Keep dropdown options readable on dark theme */
    select.input-ghost option {
      background-color: #2b354a;
      color: #ffffff;
    }
    .btn-cta { background:#ffffff; color:#000 !important; font-weight:600; letter-spacing:.02em; border-radius:0.45rem; border:1px solid rgba(255,255,255,0.12); padding:0.65rem 1rem; font-size:1rem; transition:160ms ease-in-out; }
    .btn-cta:hover { transform: translateY(-2px); background:#f1f1f1; }
  </style>
</head>
<body class="theme-gradient d-flex flex-column min-vh-100">
  <nav class="navbar navbar-expand-sm navbar-dark bg-dark custom-navbar">
    <div class="container-fluid">
      <a class="navbar-brand brand-logo" href="/">
        <span class="brand-main">PokeVault</span>
        <span class="brand-sub">SINGAPORE</span>
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar"><span class="navbar-toggler-icon"></span></button>
      <div class="collapse navbar-collapse" id="collapsibleNavbar">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item d-flex align-items-center me-3"><span class="text-light small">Signed in as <strong><%= user.username %></strong></span></li>
          <li class="nav-item ms-2"><a class="btn btn-outline-light btn-sm btn-cta btn-minimal" href="/purchases?tab=COMPLETED">Back</a></li>
          <li class="nav-item ms-2"><a class="btn btn-outline-light btn-sm btn-cta btn-minimal" href="/logout">Logout</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <main class="container flex-grow-1 py-4">
    <div class="card-frosted">
      <h4 class="fw-bold mb-2">Request Refund</h4>
      <p class="text-white-50 mb-3">Order #<%= order.id %> • Total $<%= Number(order.total).toFixed(2) %></p>

      <% if (items && items.length) { %>
        <div class="d-flex flex-wrap gap-2 mb-3">
          <% items.forEach(item => { %>
            <div class="d-flex align-items-center gap-2">
              <img class="item-thumb" src="/images/<%= item.image %>" alt="<%= item.productName %>">
              <span class="small text-white-50"><%= item.productName %> × <%= item.quantity %></span>
            </div>
          <% }) %>
        </div>
      <% } %>

      <form method="POST" action="/orders/<%= order.id %>/refund">
        <div class="mb-3">
          <label class="form-label fw-semibold">Refund Reason</label>
          <input class="form-control input-ghost" type="text" name="reason" placeholder="Tell us the reason for your refund" maxlength="120" required value="<%= reason || '' %>">
        </div>
        <div class="mb-3">
          <label class="form-label fw-semibold">Details (optional)</label>
          <textarea class="form-control input-ghost" name="description" rows="4" placeholder="Add more context if needed..."></textarea>
        </div>
        <div class="d-flex gap-2">
          <button type="submit" class="btn btn-cta">Submit Refund Request</button>
          <a class="btn btn-outline-light" href="/purchases?tab=COMPLETED">Cancel</a>
        </div>
      </form>
    </div>
  </main>
</body>
</html>
