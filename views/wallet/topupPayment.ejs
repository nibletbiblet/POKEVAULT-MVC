<!--
 I declare that this code was written by me. 
 I will not copy or allow others to copy my code. 
 I understand that copying code is considered as plagiarism.
 
 Student Name: NGJINHENG 
 Student ID: 24024323 
 Class: C372-003-E63C
 Date created: 1/2/2026
  -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link href="/theme.css" rel="stylesheet">
  <script src="https://www.paypal.com/sdk/js?client-id=<%= process.env.PAYPAL_CLIENT_ID %>&currency=SGD"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>Complete Top Up</title>
  <style>
    body {
      background-size: cover;
      background-position: center center;
      background-repeat: no-repeat;
      background-attachment: fixed;
      background-color: #111213;
      color: #fff;
      font-family: 'Montserrat', system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
    }
    body::before { content:''; position:fixed; inset:0; background:rgba(0,0,0,0.45); pointer-events:none; z-index:-1; }
    .custom-navbar { background-color: transparent !important; padding-top:1.05rem; padding-bottom:1.05rem; min-height:80px; z-index:1030; box-shadow:none; }
    .custom-navbar .navbar-brand { font-weight:700; letter-spacing:-0.01em; }
    .brand-logo { font-family:'Montserrat', system-ui, sans-serif; display:inline-flex; flex-direction:column; text-align:center; text-decoration:none; line-height:1.05; padding-left:0.1rem; }
    .brand-main { font-size:1.45rem; font-weight:700; letter-spacing:0.33em; text-transform:uppercase; color:#ffffff; }
    .brand-sub { font-size:0.68rem; font-weight:500; letter-spacing:0.28em; text-transform:uppercase; color:rgba(255,255,255,0.75); margin-top:0.15rem; }
    .card-frosted {
      background: rgba(255,255,255,0.05);
      border: 1px solid rgba(255,255,255,0.08);
      border-radius: 0.9rem;
      -webkit-backdrop-filter: blur(12px);
      backdrop-filter: blur(12px);
      box-shadow: 0 12px 34px rgba(0,0,0,0.45);
      padding: 1.5rem;
      color: #fff;
    }
    .summary-box {
      background: rgba(255,255,255,0.06);
      border: 1px solid rgba(255,255,255,0.12);
      border-radius: 0.8rem;
      padding: 1rem;
    }
    .summary-row { display:flex; justify-content:space-between; margin-bottom:0.5rem; font-size:0.95rem; }
    .total-row { font-weight:700; font-size:1.05rem; }
    .method-card {
      border: 1px solid rgba(255,255,255,0.12);
      border-radius: 0.8rem;
      padding: 1rem;
      margin-bottom: 1rem;
      background: rgba(255,255,255,0.04);
    }
    .method-header { display:flex; align-items:center; gap:0.75rem; margin-bottom:0.5rem; }
    .method-desc { font-size:0.85rem; color: rgba(255,255,255,0.65); }
    .nets-btn { border-radius: 12px; padding: 14px; font-weight: 600; }
    .secure-note { text-align:center; font-size:0.8rem; color: rgba(255,255,255,0.6); margin-top: 1rem; }
  </style>
</head>
<body class="theme-gradient d-flex flex-column min-vh-100">
  <nav class="navbar navbar-expand-sm navbar-dark bg-dark custom-navbar">
    <div class="container-fluid">
      <a class="navbar-brand brand-logo" href="/">
        <span class="brand-main">PokeVault</span>
        <span class="brand-sub">SINGAPORE</span>
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar" aria-controls="collapsibleNavbar" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="collapsibleNavbar">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item d-flex align-items-center me-3">
            <span class="text-light small">Signed in as <strong><%= user.username %></strong></span>
          </li>
          <li class="nav-item me-2">
            <a class="btn btn-outline-light btn-sm btn-cta btn-minimal" href="/wallet/topup">Back to Top Up</a>
          </li>
          <li class="nav-item">
            <a class="btn btn-outline-light btn-sm btn-cta btn-minimal" href="/logout">Logout</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <main class="container flex-grow-1 py-4">
    <div class="row justify-content-center">
      <div class="col-lg-7">
        <div class="text-center mb-4">
          <h3 class="fw-bold">Complete Your Top-Up</h3>
          <p class="text-white-50 mb-0">Choose a secure payment method to add balance to PokeVault Pay.</p>
        </div>

        <div class="card-frosted summary-box mb-4">
          <div class="summary-row">
            <span>Top-up Amount</span>
            <strong>$<%= Number(topup.amount).toFixed(2) %></strong>
          </div>
          <div class="summary-row">
            <span>Bonus</span>
            <strong class="text-success">+$<%= Number(topup.bonus || 0).toFixed(2) %></strong>
          </div>
          <hr>
          <div class="summary-row total-row">
            <span>Total Balance</span>
            <span>$<%= Number(topup.totalCoins).toFixed(2) %></span>
          </div>
        </div>

        <div class="card-frosted">
          <div class="method-card">
            <div class="method-header">
              <div class="fw-bold">PayPal</div>
              <div class="method-desc">Pay securely using PayPal or a linked card.</div>
            </div>
            <div id="paypal-button-container"></div>
          </div>

          <div class="method-card">
            <div class="method-header">
              <div class="fw-bold">NETS QR</div>
              <div class="method-desc">Scan and pay using your local banking app.</div>
            </div>
            <form method="POST" action="/wallet/topup/nets">
              <button class="btn btn-outline-light w-100 nets-btn" type="submit">Pay with NETS QR</button>
            </form>
          </div>

          <div class="secure-note">All payments are encrypted and securely processed.</div>
        </div>
      </div>
    </div>
  </main>

  <script>
    if (window.paypal) {
      paypal.Buttons({
        createOrder() {
          return fetch('/wallet/topup/paypal/create-order', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' }
          })
            .then(res => res.json())
            .then(data => {
              if (data.error) throw new Error(data.error);
              return data.id;
            });
        },
        onApprove(data) {
          return fetch('/wallet/topup/paypal/capture', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ orderID: data.orderID })
          })
            .then(res => res.json())
            .then(data => {
              if (data.redirect) {
                window.location.href = data.redirect;
              } else {
                window.location.href = '/wallet';
              }
            });
        },
        onError(err) {
          console.error('PayPal error:', err);
          alert('PayPal payment failed.');
        }
      }).render('#paypal-button-container');
    }
  </script>
</body>
</html>
