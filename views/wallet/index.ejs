<!--
 I declare that this code was written by me. 
 I will not copy or allow others to copy my code. 
 I understand that copying code is considered as plagiarism.
 
 Student Name: NGJINHENG 
 Student ID: 24024323 
 Class: C372-003-E63C
 Date created: 1/2/2026
  -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link href="/theme.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>PokeVault Pay</title>
  <style>
    body {
      background-size: cover;
      background-position: center center;
      background-repeat: no-repeat;
      background-attachment: fixed;
      background-color: #111213;
      color: #fff;
      font-family: 'Montserrat', system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
    }
    body::before { content:''; position:fixed; inset:0; background:rgba(0,0,0,0.45); pointer-events:none; z-index:-1; }
    .custom-navbar { background-color: transparent !important; padding-top:1.05rem; padding-bottom:1.05rem; min-height:80px; z-index:1030; box-shadow:none; }
    .custom-navbar .navbar-brand { font-weight:700; letter-spacing:-0.01em; }
    .brand-logo { font-family:'Montserrat', system-ui, sans-serif; display:inline-flex; flex-direction:column; text-align:center; text-decoration:none; line-height:1.05; padding-left:0.1rem; }
    .brand-main { font-size:1.45rem; font-weight:700; letter-spacing:0.33em; text-transform:uppercase; color:#ffffff; }
    .brand-sub { font-size:0.68rem; font-weight:500; letter-spacing:0.28em; text-transform:uppercase; color:rgba(255,255,255,0.75); margin-top:0.15rem; }
    .card-frosted {
      background: rgba(255,255,255,0.05);
      border: 1px solid rgba(255,255,255,0.08);
      border-radius: 0.8rem;
      -webkit-backdrop-filter: blur(12px);
      backdrop-filter: blur(12px);
      box-shadow: 0 12px 34px rgba(0,0,0,0.45);
      padding: 1.5rem;
      color: #fff;
    }
    .balance-box {
      background: linear-gradient(135deg, rgba(74,212,255,0.25), rgba(15,176,255,0.08));
      border: 1px solid rgba(74,212,255,0.3);
      border-radius: 0.75rem;
      padding: 1.25rem;
      text-align: center;
      box-shadow: 0 12px 28px rgba(0,0,0,0.45);
    }
    .balance-amount { font-size: 2rem; font-weight: 700; letter-spacing: -0.01em; }
    .btn-cta { background:#ffffff; color:#000 !important; font-weight:600; letter-spacing:.02em; border-radius:0.45rem; border:1px solid rgba(255,255,255,0.12); padding:0.65rem 1rem; font-size:1rem; transition:160ms ease-in-out; }
    .btn-cta:hover { transform: translateY(-2px); background:#f1f1f1; }
    .txn-row { border-bottom: 1px solid rgba(255,255,255,0.08); padding: 0.75rem 0; }
    .txn-type { font-weight: 600; }
    .credit { color: #60f0c0; }
    .debit { color: #ff7f7f; }
  </style>
</head>
<body class="theme-gradient d-flex flex-column min-vh-100">
  <nav class="navbar navbar-expand-sm navbar-dark bg-dark custom-navbar">
    <div class="container-fluid">
      <a class="navbar-brand brand-logo" href="/">
        <span class="brand-main">PokeVault</span>
        <span class="brand-sub">SINGAPORE</span>
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar" aria-controls="collapsibleNavbar" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="collapsibleNavbar">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item d-flex align-items-center me-3">
            <span class="text-light small">Signed in as <strong><%= user.username %></strong></span>
          </li>
          <li class="nav-item me-2">
            <a class="btn btn-outline-light btn-sm btn-cta btn-minimal" href="/shopping">Shop</a>
          </li>
          <li class="nav-item me-2">
            <a class="btn btn-outline-light btn-sm btn-cta btn-minimal" href="/purchases">My Purchases</a>
          </li>
          <li class="nav-item">
            <a class="btn btn-outline-light btn-sm btn-cta btn-minimal" href="/logout">Logout</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <main class="container flex-grow-1 py-4">
    <% if (messages && messages.length) { %>
      <div class="alert alert-danger">
        <% messages.forEach(m => { %><div><%= m %></div><% }) %>
      </div>
    <% } %>
    <% if (success && success.length) { %>
      <div class="alert alert-success">
        <% success.forEach(m => { %><div><%= m %></div><% }) %>
      </div>
    <% } %>
    <div class="row g-3">
      <div class="col-lg-4">
        <div class="card-frosted">
          <div class="balance-box mb-3">
            <div class="text-uppercase small text-white-50">Available Balance</div>
            <div class="balance-amount mt-2">$<%= Number(balance || 0).toFixed(2) %></div>
            <div class="small text-white-50 mt-1">Stored-value balance (SGD)</div>
          </div>
          <a href="/wallet/topup" class="btn btn-cta w-100">Top Up Balance</a>
          <hr class="border-secondary">
          <div class="small text-white-50 mb-2">Withdraw Funds</div>
          <form action="/wallet/withdraw" method="POST" class="d-grid gap-2">
            <input type="number" min="0.01" step="0.01" name="amount" class="form-control" placeholder="Amount (SGD)" required>
            <input type="text" name="destination" class="form-control" placeholder="Destination (bank/paynow)" required>
            <button type="submit" class="btn btn-outline-light btn-sm">Withdraw</button>
          </form>
          <hr class="border-secondary">
          <div class="small text-white-50 mb-2">Transfer to Another User</div>
          <form action="/wallet/transfer" method="POST" class="d-grid gap-2">
            <input type="email" name="recipientEmail" class="form-control" placeholder="Recipient email" required>
            <input type="number" min="0.01" step="0.01" name="amount" class="form-control" placeholder="Amount (SGD)" required>
            <button type="submit" class="btn btn-outline-light btn-sm">Transfer</button>
          </form>
          <div class="text-white-50 small mt-2">Transfers of $<%= Number(amlAlertThreshold || 0).toFixed(0) %> and above are compliance-flagged.</div>
        </div>
      </div>
      <div class="col-lg-8">
        <div class="card-frosted">
          <h5 class="fw-bold mb-3">Transaction History</h5>
          <% if (!transactions || !transactions.length) { %>
            <div class="text-white-50">No wallet transactions yet.</div>
          <% } else { %>
            <% transactions.forEach(txn => { %>
              <div class="txn-row d-flex justify-content-between align-items-center">
                <div>
                  <div class="txn-type"><%= String(txn.type || '').replace('_', ' ') %></div>
                  <div class="text-white-50 small"><%= new Date(txn.created_at).toLocaleString() %></div>
                  <% if (txn.reference) { %>
                    <div class="text-white-50 small">Ref: <%= txn.reference %></div>
                  <% } %>
                </div>
                <div class="<%= Number(txn.amount) >= 0 ? 'credit' : 'debit' %> fw-bold">
                  <%= Number(txn.amount) >= 0 ? '+' : '' %><%= Number(txn.amount).toFixed(2) %>
                </div>
              </div>
            <% }) %>
          <% } %>
        </div>
      </div>
    </div>
  </main>
</body>
</html>
