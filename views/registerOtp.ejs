<!--
 I declare that this code was written by me. 
 I will not copy or allow others to copy my code. 
 I understand that copying code is considered as plagiarism.
 
 Student Name: Nate
 Student ID: 24025215
 Class: C372-003-E63C
 Date created: 1/2/2026
  -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link href="/theme.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>PokeVault - Verify OTP</title>
  <style>
    .custom-navbar {
      background-color: transparent !important;
      padding-top: 1.05rem;
      padding-bottom: 1.05rem;
      min-height: 80px;
      z-index: 1030;
      box-shadow: none;
    }
    .brand-logo {
      font-family: 'Montserrat', system-ui, sans-serif;
      display: inline-flex;
      flex-direction: column;
      text-align: center;
      text-decoration: none;
      line-height: 1.05;
      padding-left: 0.1rem;
    }
    .brand-main {
      font-size: 1.45rem;
      font-weight: 700;
      letter-spacing: 0.33em;
      text-transform: uppercase;
      color: #ffffff;
    }
    .brand-sub {
      font-size: 0.68rem;
      font-weight: 500;
      letter-spacing: 0.28em;
      text-transform: uppercase;
      color: rgba(255,255,255,0.75);
      margin-top: 0.15rem;
    }
    .auth-hero {
      position: relative;
      margin: 3.5rem auto;
      max-width: 720px;
      background: linear-gradient(135deg, #070c14 0%, #050a12 45%, #02050d 100%);
      border-radius: 16px;
      border: 1px solid rgba(88, 166, 255, 0.35);
      box-shadow:
        0 0 18px rgba(88,166,255,0.45),
        0 25px 80px rgba(0,0,0,0.75);
      overflow: hidden;
      color: #eaf2ff;
      padding: 2.2rem 2.6rem;
    }
    .auth-heading { font-weight: 700; font-size: 1.8rem; letter-spacing: -0.01em; }
    .auth-subtitle { color: rgba(234,242,255,0.78); margin-bottom: 1.1rem; }
    .form-label { color: rgba(234,242,255,0.85); font-weight: 600; letter-spacing: 0.01em; }
    .form-control {
      background: rgba(255,255,255,0.03);
      border: 1px solid rgba(255,255,255,0.16);
      color: #eaf2ff;
      border-radius: 999px;
      padding-left: 1rem;
      height: 44px;
      text-align: center;
      letter-spacing: 0.3em;
      font-weight: 700;
    }
    .btn-glow {
      width: 100%;
      border-radius: 999px;
      border: 1px solid rgba(255,255,255,0.18);
      background: linear-gradient(90deg, #0fb0ff 0%, #4ad4ff 100%);
      color: #04101f;
      font-weight: 700;
      letter-spacing: 0.01em;
      box-shadow: 0 10px 28px rgba(15,176,255,0.35);
      transition: transform 0.15s ease, box-shadow 0.15s ease;
    }
    .btn-glow:hover {
      transform: translateY(-1px);
      box-shadow: 0 14px 32px rgba(15,176,255,0.42);
    }
    .btn-linkish {
      background: transparent;
      border: 1px solid rgba(255,255,255,0.2);
      color: #eaf2ff;
      border-radius: 999px;
      padding: 0.5rem 1.2rem;
      font-weight: 600;
    }
    .meta { color: rgba(234,242,255,0.72); font-size: 0.95rem; }
  </style>
</head>
<body class="theme-gradient d-flex flex-column min-vh-100">
  <nav class="navbar navbar-expand-sm navbar-dark bg-dark custom-navbar">
    <div class="container-fluid">
      <a class="navbar-brand brand-logo" href="/">
        <span class="brand-main">PokeVault</span>
        <span class="brand-sub">SINGAPORE</span>
      </a>
    </div>
  </nav>

  <main class="container flex-grow-1">
    <div class="auth-hero">
      <h2 class="auth-heading mb-1">Verify OTP</h2>
      <p class="auth-subtitle">Enter the 6-digit code sent to <strong><%= emailMasked %></strong>.</p>

      <% if (messages && messages.length > 0) { %>
        <div class="alert alert-danger">
          <% messages.forEach(function(message) { %>
            <div><%= message %></div>
          <% }); %>
        </div>
      <% } %>

      <form action="/register/otp" method="POST" class="mt-3">
        <div class="mb-3">
          <label for="otp" class="form-label">One-Time Password</label>
          <input type="text" id="otp" name="otp" class="form-control" maxlength="6" inputmode="numeric" pattern="[0-9]{6}" required>
        </div>
        <button type="submit" class="btn btn-glow mt-2">Verify &amp; Create Account</button>
      </form>

      <div class="d-flex flex-column align-items-center gap-2 mt-4">
        <div class="meta">Didn't receive the email?</div>
        <form action="/register/otp/resend" method="POST">
          <button type="submit" class="btn-linkish">Resend OTP</button>
        </form>
        <div class="meta">You can request a new OTP every <%= resendSeconds %> seconds.</div>
      </div>
    </div>
  </main>
</body>
</html>
