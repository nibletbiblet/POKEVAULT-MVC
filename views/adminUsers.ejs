<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link href="/theme.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>Admin - Users</title>
  <style>
    body { background:#111213; color:#fff; font-family:'Montserrat',system-ui,-apple-system,'Segoe UI',Roboto,'Helvetica Neue',Arial,sans-serif;  background-size:cover; background-repeat:no-repeat; background-attachment:fixed; }
    body::before { content:''; position:fixed; inset:0; background:rgba(0,0,0,0.45); z-index:-1; }
    .custom-navbar { background-color:transparent !important; padding-top:1.05rem; padding-bottom:1.05rem; min-height:80px; z-index:1030; box-shadow:none; }
    .custom-navbar .navbar-brand { font-weight:700; letter-spacing:-0.01em; }
    .card-frosted { background:rgba(255,255,255,0.05); border:1px solid rgba(255,255,255,0.08); border-radius:0.8rem; -webkit-backdrop-filter:blur(12px); backdrop-filter:blur(12px); box-shadow:0 12px 34px rgba(0,0,0,0.45); padding:1.2rem; }
    .brand-logo { font-family:'Montserrat',system-ui,sans-serif; display:inline-flex; flex-direction:column; text-align:center; text-decoration:none; line-height:1.05; padding-left:0.1rem; }
    .brand-main { font-size:1.45rem; font-weight:700; letter-spacing:0.33em; text-transform:uppercase; color:#ffffff; }
    .brand-sub { font-size:0.68rem; font-weight:500; letter-spacing:0.28em; text-transform:uppercase; color:rgba(255,255,255,0.75); margin-top:0.15rem; }
    footer.site-footer { background:#0b0c0d; color:#fff; padding:2.5rem 0; border-top:1px solid rgba(255,255,255,0.03); }
    a, .nav-link { color: rgba(255,255,255,0.95); }
    .user-card {
      background: rgba(255,255,255,0.05);
      border: 1px solid rgba(255,255,255,0.1);
      border-radius: 0.8rem;
      padding: 1rem;
      box-shadow: 0 12px 34px rgba(0,0,0,0.45);
      transition: transform 0.15s ease, box-shadow 0.15s ease, border-color 0.15s ease;
      height: 100%;
    }
    .user-card:hover {
      transform: translateY(-4px);
      border-color: rgba(255,255,255,0.2);
      box-shadow: 0 16px 38px rgba(0,0,0,0.5);
    }
    .role-badge {
      display: inline-flex;
      align-items: center;
      padding: 0.28rem 0.65rem;
      border-radius: 999px;
      border: 1px solid rgba(255,255,255,0.12);
      font-weight: 700;
      font-size: 0.8rem;
      letter-spacing: 0.03em;
      text-transform: uppercase;
      background: rgba(255,255,255,0.08);
      color: #fff;
    }
    .role-badge.admin { background: rgba(220,53,69,0.18); border-color: rgba(220,53,69,0.5); }
    .user-meta { color: rgba(255,255,255,0.7); font-size: 0.9rem; }
    .user-actions { display: grid !important; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 0.5rem; }
    .user-actions > a,
    .user-actions > form,
    .user-actions > span { width: 100%; }
    .user-actions > form .btn { width: 100%; }
    .user-actions > span {
      display: flex;
      align-items: center;
      justify-content: center;
      text-align: center;
      font-size: 0.85rem;
      padding: 0.25rem 0.4rem;
    }

    .search-box {
      background: rgba(255,255,255,0.05);
      border: 1px solid rgba(255,255,255,0.14);
      border-radius: 999px;
      -webkit-backdrop-filter: blur(12px);
      backdrop-filter: blur(12px);
      padding: 0.65rem 1rem;
      color: #fff;
      box-shadow: 0 10px 28px rgba(0,0,0,0.35);
    }
    .search-box::placeholder { color: rgba(255,255,255,0.6); }
    .filter-select {
      background: rgba(255,255,255,0.05);
      border: 1px solid rgba(255,255,255,0.12);
      border-radius: 999px;
      -webkit-backdrop-filter: blur(10px);
      backdrop-filter: blur(10px);
      color: #fff;
      padding: 0.65rem 1rem;
      font-weight: 600;
      letter-spacing: 0.01em;
      appearance: none;
      background-image:
        linear-gradient(180deg, rgba(255,255,255,0.08), rgba(255,255,255,0.02)),
        url("data:image/svg+xml,%3Csvg width='18' height='12' viewBox='0 0 18 12' fill='none' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M2 4l7 7 7-7' stroke='%23ffffff' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'/%3E%3C/svg%3E");
      background-repeat: no-repeat, no-repeat;
      background-position: 0 0, calc(100% - 0.9rem) center;
      background-size: auto, 18px 12px;
      box-shadow: 0 8px 24px rgba(0,0,0,0.35);
    }
    .filter-select:focus {
      border-color: rgba(255,255,255,0.28);
      outline: none;
      box-shadow: 0 0 0 3px rgba(255,255,255,0.14);
    }
    .filter-select option {
      background: #0f1011;
      color: #fff;
    }

    /* Toast notification styles */
    .toast-container {
      position: fixed;
      top: 20px;
      right: 20px;
      z-index: 2000;
    }
    .toast-notification {
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(8px);
      color: #000000;
      padding: 1rem 1.5rem;
      border-radius: 0.5rem;
      box-shadow: 0 8px 24px rgba(0,0,0,0.4);
      margin-bottom: 0.75rem;
      animation: slideIn 300ms ease-out, slideOut 300ms ease-out 2700ms forwards;
      border: 1px solid rgba(255,255,255,0.1);
      font-weight: 500;
    }
    @keyframes slideIn { from { transform: translateX(400px); opacity: 0; } to { transform: translateX(0); opacity: 1; } }
    @keyframes slideOut { from { transform: translateX(0); opacity: 1; } to { transform: translateX(400px); opacity: 0; } }
  </style>
</head>
<body class="theme-gradient d-flex flex-column min-vh-100">
  <div class="toast-container" id="toastContainer"></div>
  <nav class="navbar navbar-expand-sm navbar-dark bg-dark custom-navbar">
    <div class="container-fluid">
      <a class="navbar-brand brand-logo" href="/">
        <span class="brand-main">PokeVault</span>
        <span class="brand-sub">SINGAPORE</span>
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar"><span class="navbar-toggler-icon"></span></button>
      <div class="collapse navbar-collapse" id="collapsibleNavbar">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item d-flex align-items-center me-3"><span class="text-light small">Signed in as <strong><%= user.username %></strong></span></li>
          <li class="nav-item ms-2"><a class="btn btn-outline-light btn-sm btn-cta btn-minimal" href="/">Home</a></li>
          <li class="nav-item ms-2"><a class="btn btn-outline-light btn-sm btn-cta btn-minimal" href="/inventory">Inventory</a></li>
          <li class="nav-item ms-2"><a class="btn btn-outline-light btn-sm btn-cta btn-minimal" href="/logout">Logout</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <main class="container flex-grow-1 py-4">
    <div class="card-frosted">
      <div class="d-flex justify-content-between align-items-center mb-3">
        <h4 class="fw-bold mb-0">All Users</h4>
        <div class="d-flex gap-2">
          <a class="btn btn-outline-light btn-sm" href="/admin/kyc">KYC Review</a>
        </div>
      </div>
      <div class="row g-2 mb-3">
        <div class="col-12 col-md-6 col-lg-4">
          <input id="userSearch" type="search" class="form-control form-control-sm search-box" placeholder="Search name or email..." aria-label="Search users">
        </div>
        <div class="col-12 col-md-4 col-lg-3">
          <select id="roleFilter" class="form-select form-select-sm filter-select" aria-label="Filter by role">
            <option value="">All roles</option>
            <option value="admin">Admin</option>
            <option value="user">User</option>
          </select>
        </div>
      </div>
      <div class="row g-3">
        <% users.forEach(u => { %>
          <div class="col-12 col-md-6 col-lg-4 user-card-wrapper" data-search="<%= (u.username + ' ' + u.email).toLowerCase() %>" data-role="<%= u.role %>">
            <div class="user-card d-flex flex-column">
              <div class="d-flex justify-content-between align-items-start mb-2">
                <div>
                  <div class="fw-bold"><%= u.id %>. <%= u.username %></div>
                  <div class="user-meta"><%= u.email %></div>
                </div>
                <span class="role-badge <%= u.role === 'admin' ? 'admin' : '' %>"><%= u.role %></span>
              </div>
              <% const isBanned = u.isBanned === 1 || u.isBanned === '1' || u.isBanned === true; %>
              <% const hasHighRefundRate = (u.refundRate || 0) >= 0.5 && (u.totalOrders || 0) > 0; %>
              <div class="mb-2 user-meta">
                Status:
                <% if (isBanned) { %>
                  <span class="badge bg-danger">Banned</span>
                <% } else { %>
                  <span class="badge bg-success">Active</span>
                <% } %>
                <% if (hasHighRefundRate) { %>
                  <span class="badge bg-warning text-dark ms-2" title="User has an unusually high refund rate">High refund rate</span>
                <% } %>
              </div>
              <div class="mb-2 user-meta">Contact: <%= u.contact || '-' %></div>
              <div class="mb-3 user-meta">Address: <%= u.address || '-' %></div>
              <div class="user-actions d-flex gap-2 mt-auto">
                <% const isSelfAdmin = user && user.id === u.id && u.role === 'admin'; %>
                <% if (u.role !== 'admin') { %>
                  <a class="btn btn-outline-light btn-sm" href="/admin/users/<%= u.id %>/orders">View Orders</a>
                  <a class="btn btn-outline-light btn-sm" href="/admin/refunds?userId=<%= u.id %>">Refund History</a>
                  <a class="btn btn-outline-light btn-sm" href="/admin/users/<%= u.id %>/edit">Edit</a>
                  <a class="btn btn-outline-light btn-sm" href="/admin/users/<%= u.id %>/suspension-history">Suspension History</a>
                  <form action="/admin/users/<%= u.id %>/delete" method="POST" class="d-inline-flex flex-grow-1 delete-user-form" data-name="<%= u.username %>">
                    <input type="hidden" name="adminKey" value="">
                    <button type="submit" class="btn btn-danger btn-sm w-100" title="Delete user">Delete</button>
                  </form>
                <% } else if (isSelfAdmin) { %>
                  <a class="btn btn-outline-light btn-sm" href="/admin/users/<%= u.id %>/edit">Edit Profile</a>
                  <a class="btn btn-outline-light btn-sm" href="/admin/activity?adminId=<%= u.id %>">Activity</a>
                  <span class="text-white-50 small">Editing own admin</span>
                <% } else { %>
                  <a class="btn btn-outline-light btn-sm" href="/admin/activity?adminId=<%= u.id %>">Activity</a>
                  <span class="text-white-50 small">Admin account</span>
                <% } %>
              </div>
              <% if (u.role !== 'admin') { %>
                <div class="mt-2">
                  <% if (!isBanned) { %>
                    <form action="/admin/users/<%= u.id %>/ban" method="POST" class="d-flex gap-2">
                      <input type="text" name="banReason" class="form-control form-control-sm" placeholder="Ban reason (optional)">
                      <button type="submit" class="btn btn-warning btn-sm">Ban</button>
                    </form>
                  <% } else { %>
                    <form action="/admin/users/<%= u.id %>/unban" method="POST">
                      <button type="submit" class="btn btn-success btn-sm">Unban</button>
                    </form>
                  <% } %>
                </div>
              <% } %>
            </div>
          </div>
        <% }) %>
      </div>
    </div>
  </main>

  <script>
    // Client-side filter for users by search term and role
    (function() {
      const searchInput = document.getElementById('userSearch');
      const roleSelect = document.getElementById('roleFilter');
      if (!searchInput || !roleSelect) return;

      const cards = Array.from(document.querySelectorAll('.user-card-wrapper'));

      function filterCards() {
        const term = searchInput.value.trim().toLowerCase();
        const role = roleSelect.value.trim();
        cards.forEach(card => {
          const termSpace = card.dataset.search || '';
          const cardRole = card.dataset.role || '';
          const matchesTerm = termSpace.includes(term);
          const matchesRole = !role || cardRole === role;
          card.style.display = (matchesTerm && matchesRole) ? '' : 'none';
        });
      }

      searchInput.addEventListener('input', filterCards);
      roleSelect.addEventListener('change', filterCards);
    })();

    // Toast helper
    function showToast(message) {
      const container = document.getElementById('toastContainer');
      if (!container) return;
      const toast = document.createElement('div');
      toast.className = 'toast-notification';
      toast.textContent = message;
      container.appendChild(toast);
      setTimeout(() => toast.remove(), 3200);
    }

    // AJAX delete for users (non-admin)
    (function() {
      const forms = Array.from(document.querySelectorAll('.delete-user-form'));
      if (!forms.length) return;
      const keyModal = document.getElementById('adminKeyModal');
      const keyInput = document.getElementById('adminKeyInput');
      const keyError = document.getElementById('adminKeyError');
      const confirmBtn = document.getElementById('adminKeyConfirm');
      let pendingForm = null;
      const bootstrapModal = keyModal ? new bootstrap.Modal(keyModal) : null;

      forms.forEach(form => {
        form.addEventListener('submit', async (e) => {
          e.preventDefault();
          pendingForm = form;
          if (keyError) keyError.textContent = '';
          if (keyInput) keyInput.value = '';
          if (bootstrapModal) {
            bootstrapModal.show();
          } else {
            const adminKey = prompt('Please enter admin key to confirm this action.');
            if (!adminKey) return;
            if (!confirm('Are you sure you want to proceed? This action cannot be undone.')) return;
            const input = pendingForm.querySelector('input[name="adminKey"]');
            if (input) input.value = adminKey;
            pendingForm.submit();
          }
        });
      });

      if (confirmBtn) {
        confirmBtn.addEventListener('click', () => {
          if (!pendingForm) return;
          const adminKey = keyInput ? keyInput.value.trim() : '';
          if (!adminKey) {
            if (keyError) keyError.textContent = 'Admin key is required.';
            return;
          }
          if (!confirm('Are you sure you want to proceed? This action cannot be undone.')) return;
          const input = pendingForm.querySelector('input[name="adminKey"]');
          if (input) input.value = adminKey;
          pendingForm.submit();
        });
      }
    })();
  </script>
  <div class="modal fade" id="adminKeyModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
      <div class="modal-content bg-dark text-white">
        <div class="modal-header border-0">
          <h5 class="modal-title">Admin Authority Confirmation</h5>
          <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body">
          <p class="mb-2">Please enter admin key to confirm this action.</p>
          <input type="password" id="adminKeyInput" class="form-control" placeholder="Admin key">
          <div id="adminKeyError" class="text-danger small mt-2"></div>
        </div>
        <div class="modal-footer border-0">
          <button type="button" class="btn btn-outline-light btn-sm" data-bs-dismiss="modal">Cancel</button>
          <button type="button" class="btn btn-danger btn-sm" id="adminKeyConfirm">Confirm</button>
        </div>
      </div>
    </div>
  </div>
  <footer class="site-footer mt-auto">
  <div class="container text-center">
    <p class="mb-1">Connecting Pokémon card collectors worldwide — shop, sell, and trade in a safe and growing community.</p>
    <small class="small">© <%= new Date().getFullYear() %> PokéVault. All rights reserved.</small>
  </div>
</footer>
</body>
</html>

