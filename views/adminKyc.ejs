<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link href="/theme.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>Admin - KYC Review</title>
  <style>
    body { background:#0d0f12; color:#fff; font-family:'Montserrat',system-ui,-apple-system,'Segoe UI',Roboto,'Helvetica Neue',Arial,sans-serif; background-size:cover; background-repeat:no-repeat; background-attachment:fixed; }
    body::before { content:''; position:fixed; inset:0; background:rgba(0,0,0,0.45); z-index:-1; }
    .card-frosted { background:rgba(255,255,255,0.04); border:1px solid rgba(255,255,255,0.08); border-radius:1rem; -webkit-backdrop-filter:blur(14px); backdrop-filter:blur(14px); box-shadow:0 18px 48px rgba(0,0,0,0.55); padding:1.4rem; }
    .table thead th { border-color: rgba(255,255,255,0.08); color: rgba(255,255,255,0.85); text-transform: uppercase; letter-spacing: 0.02em; font-size: 0.82rem; background: rgba(255,255,255,0.04); }
    .table tbody td { border-color: rgba(255,255,255,0.05); color: rgba(255,255,255,0.92); vertical-align: middle; }
    .table-dark tbody tr { background: rgba(8, 24, 48, 0.62) !important; }
  </style>
</head>
<body class="theme-gradient d-flex flex-column min-vh-100">
  <main class="container flex-grow-1 py-4">
    <div class="card-frosted">
      <div class="d-flex justify-content-between align-items-center mb-3">
        <h4 class="fw-bold mb-0">KYC Review</h4>
        <a class="btn btn-outline-light btn-sm" href="/admin/users">Back to Users</a>
      </div>

      <% if (messages && messages.length) { %>
        <div class="alert alert-danger"><%= messages[0] %></div>
      <% } %>
      <% if (success && success.length) { %>
        <div class="alert alert-success"><%= success[0] %></div>
      <% } %>

      <% if (!users || !users.length) { %>
        <p class="text-white-50 mb-0">No pending KYC requests.</p>
      <% } else { %>
        <div class="table-responsive">
          <table class="table table-dark table-hover align-middle mb-0">
            <thead>
              <tr>
                <th>User</th>
                <th>Email</th>
                <th>Submitted</th>
                <th>Actions</th>
              </tr>
            </thead>
            <tbody>
              <% users.forEach(u => { %>
                <tr>
                  <td><%= u.username %></td>
                  <td><%= u.email %></td>
                  <td><%= u.kycUpdatedAt ? new Date(u.kycUpdatedAt).toLocaleString() : '-' %></td>
                  <td>
                    <form method="POST" action="/admin/kyc/<%= u.id %>/approve" class="d-inline">
                      <button class="btn btn-success btn-sm" type="submit">Approve</button>
                    </form>
                    <form method="POST" action="/admin/kyc/<%= u.id %>/reject" class="d-inline">
                      <input type="text" name="reason" class="form-control form-control-sm d-inline-block" style="width:180px" placeholder="Reason (optional)">
                      <button class="btn btn-danger btn-sm" type="submit">Reject</button>
                    </form>
                  </td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        </div>
      <% } %>
    </div>
  </main>
</body>
</html>
