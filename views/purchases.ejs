<!--
 I declare that this code was written by me. 
 I will not copy or allow others to copy my code. 
 I understand that copying code is considered as plagiarism.
 
 Student Name: NGJINHENG , Nate
 Student ID: 24024323 , 24025215
 Class: C372-003-E63C
 Date created: 1/2/2026
  -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link href="/theme.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>My Purchases</title>
  <style>
    body {
      background:#111213;
      color:#fff;
      font-family:'Montserrat',system-ui,-apple-system,'Segoe UI',Roboto,'Helvetica Neue',Arial,sans-serif;
      background-size:cover;
      background-repeat:no-repeat;
      background-attachment:fixed;
    }
    body::before { content:''; position:fixed; inset:0; background:rgba(0,0,0,0.45); z-index:-1; }
    .custom-navbar { background-color:transparent !important; padding-top:1.05rem; padding-bottom:1.05rem; min-height:80px; z-index:1030; box-shadow:none; }
    .custom-navbar .navbar-brand { font-weight:700; letter-spacing:-0.01em; }
    .card-frosted {
      background: rgba(255,255,255,0.05);
      border: 1px solid rgba(255,255,255,0.08);
      border-radius: 0.8rem;
      -webkit-backdrop-filter: blur(12px);
      backdrop-filter: blur(12px);
      box-shadow: 0 12px 34px rgba(0,0,0,0.45);
      padding: 1.2rem;
    }
    .brand-logo { font-family:'Montserrat',system-ui,sans-serif; display:inline-flex; flex-direction:column; text-align:center; text-decoration:none; line-height:1.05; padding-left:0.1rem; }
    .brand-main { font-size:1.45rem; font-weight:700; letter-spacing:0.33em; text-transform:uppercase; color:#ffffff; }
    .brand-sub { font-size:0.68rem; font-weight:500; letter-spacing:0.28em; text-transform:uppercase; color:rgba(255,255,255,0.75); margin-top:0.15rem; }
    .nav-tabs .nav-link {
      color: rgba(255,255,255,0.8);
      border: none;
      border-bottom: 2px solid transparent;
      background: transparent;
      font-weight: 600;
    }
    .nav-tabs .nav-link.active {
      color: #fff;
      border-color: #4ad4ff;
      background: transparent;
    }
    .order-card {
      background: rgba(255,255,255,0.05);
      border: 1px solid rgba(255,255,255,0.1);
      border-radius: 0.8rem;
      padding: 1rem;
      box-shadow: 0 10px 26px rgba(0,0,0,0.35);
      transition: transform 0.15s ease, box-shadow 0.15s ease, border-color 0.15s ease;
    }
    .order-card:hover {
      transform: translateY(-4px);
      border-color: rgba(255,255,255,0.2);
      box-shadow: 0 16px 34px rgba(0,0,0,0.45);
    }
    .order-meta { color: rgba(255,255,255,0.7); font-size: 0.9rem; }
    .badge-soft {
      background: rgba(255,255,255,0.08);
      border: 1px solid rgba(255,255,255,0.12);
      color: #fff;
      border-radius: 999px;
      padding: 0.3rem 0.65rem;
      font-weight: 700;
      letter-spacing: 0.02em;
    }
    .item-thumb {
      width: 40px;
      height: 40px;
      object-fit: cover;
      border-radius: 0.45rem;
      border: 1px solid rgba(255,255,255,0.12);
      box-shadow: 0 6px 16px rgba(0,0,0,0.35);
    }
    .modal-content.card-frosted {
      background: rgba(20, 28, 48, 0.95);
      border: 1px solid rgba(255,255,255,0.12);
      color: #fff;
    }
    .btn-cta:hover {
      background: #f1f1f1;
      color: #000 !important;
    }
    .btn-cta {
      background: #ffffff;
      color: #0a1020 !important;
      border: 1px solid rgba(255,255,255,0.35);
    }
    .btn-cta:focus,
    .btn-cta:active {
      background: #dfe7ff;
      color: #0a1020 !important;
      box-shadow: 0 0 0 0.2rem rgba(77, 160, 255, 0.35);
    }
    .btn-cta:disabled,
    .btn-cta.disabled {
      background: rgba(255,255,255,0.18);
      color: rgba(255,255,255,0.75) !important;
      border-color: rgba(255,255,255,0.2);
      opacity: 1;
      cursor: not-allowed;
    }
  </style>
</head>
<body class="theme-gradient d-flex flex-column min-vh-100">
  <nav class="navbar navbar-expand-sm navbar-dark bg-dark custom-navbar">
    <div class="container-fluid">
      <a class="navbar-brand brand-logo" href="/">
        <span class="brand-main">PokeVault</span>
        <span class="brand-sub">SINGAPORE</span>
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar"><span class="navbar-toggler-icon"></span></button>
      <div class="collapse navbar-collapse" id="collapsibleNavbar">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item d-flex align-items-center me-3"><span class="text-light small">Signed in as <strong><%= user.username %></strong></span></li>
          <li class="nav-item"><a class="btn btn-outline-light btn-sm btn-cta btn-minimal" href="/">Home</a></li>
          <li class="nav-item ms-2"><a class="btn btn-outline-light btn-sm btn-cta btn-minimal" href="/shopping">Shop</a></li>
          <li class="nav-item ms-2"><a class="btn btn-outline-light btn-sm btn-cta btn-minimal" href="/logout">Logout</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <main class="container flex-grow-1 py-4">
    <div class="card-frosted">
      <div class="d-flex justify-content-between align-items-center mb-3">
        <div>
          <h4 class="fw-bold mb-1">My Purchases</h4>
          <small class="text-white-50">Track order status and receipts</small>
        </div>
        <div>
          <a class="btn btn-outline-light btn-sm" href="/shopping">Shop More</a>
        </div>
      </div>


      <ul class="nav nav-tabs">
        <li class="nav-item">
          <a class="nav-link <%= activeTab === 'TO_PAY' ? 'active' : '' %>" href="/purchases?tab=TO_PAY">To Pay</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= activeTab === 'TO_SHIP' ? 'active' : '' %>" href="/purchases?tab=TO_SHIP">To Ship</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= activeTab === 'TO_RECEIVE' ? 'active' : '' %>" href="/purchases?tab=TO_RECEIVE">To Receive</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= activeTab === 'PENDING' ? 'active' : '' %>" href="/purchases?tab=PENDING">Pending</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= activeTab === 'COMPLETED' ? 'active' : '' %>" href="/purchases?tab=COMPLETED">Completed</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= activeTab === 'REFUND' ? 'active' : '' %>" href="/purchases?tab=REFUND">Refund</a>
        </li>
      </ul>

      <% if (!orders || !orders.length) { %>
        <div class="text-center py-4">
          <p class="text-white fw-semibold mb-1">No orders in this section</p>
          <p class="text-white-50 mb-3 small">Your purchases will appear here once you complete payment.</p>
        </div>
      <% } else { %>
        <div class="row g-3 mt-1">
          <% orders.forEach(o => { %>
            <div class="col-12">
              <div class="order-card">
                <div class="d-flex flex-wrap justify-content-between align-items-start gap-3">
                  <div>
                    <div class="fw-bold">Order #<%= o.id %></div>
                    <div class="order-meta"><%= new Date(o.createdAt).toLocaleString() %></div>
                    <div class="order-meta mt-1">
                      Status:
                      <span class="badge-soft">
                        <% if (activeTab === 'REFUND') { %>
                          <%= (o.refund && o.refund.status === 'PENDING') ? 'REFUND PENDING' : 'REFUNDED' %>
                        <% } else { %>
                          <%= activeTab.replace('_', ' ') %>
                        <% } %>
                      </span>
                    </div>
                  </div>
                  <div class="text-end">
                    <div class="badge-soft">$<%= Number(o.total).toFixed(2) %></div>
                    <% if (activeTab === 'TO_PAY') { %>
                      <div class="mt-2">
                        <a class="btn btn-outline-light btn-sm" href="/orders/<%= o.id %>/resume">Continue Payment</a>
                      </div>
                    <% } else if (activeTab === 'TO_RECEIVE') { %>
                      <div class="mt-2 d-flex flex-wrap gap-2 justify-content-end">
                        <form method="POST" action="/orders/<%= o.id %>/received">
                          <button type="submit" class="btn btn-outline-light btn-sm">Mark as Received</button>
                        </form>
                        <a class="btn btn-outline-light btn-sm" href="/orders/<%= o.id %>">View Receipt</a>
                      </div>
                    <% } else if (activeTab === 'COMPLETED') { %>
                      <div class="mt-2 d-flex flex-wrap gap-2 justify-content-end">
                        <a class="btn btn-outline-light btn-sm" href="/orders/<%= o.id %>">View Receipt</a>
                        <% if (!(o.refund && o.refund.status === 'REJECTED')) { %>
                          <button
                            type="button"
                            class="btn btn-outline-light btn-sm refund-open-btn"
                            data-refund-url="/orders/<%= o.id %>/refund"
                          >Request Refund</button>
                        <% } %>
                      </div>
                    <% } else { %>
                      <div class="mt-2">
                        <a class="btn btn-outline-light btn-sm" href="/orders/<%= o.id %>">View Receipt</a>
                      </div>
                    <% } %>
                  </div>
                </div>
                <% if (o.items && o.items.length) { %>
                  <div class="mt-3 d-flex flex-wrap gap-2">
                    <% o.items.forEach(item => { %>
                      <div class="d-flex align-items-center gap-2">
                        <img class="item-thumb" src="/images/<%= item.image %>" alt="<%= item.productName %>">
                        <span class="small text-white-50"><%= item.productName %></span>
                      </div>
                    <% }) %>
                  </div>
                <% } %>
                <% if (activeTab === 'COMPLETED' && o.refund && o.refund.status === 'REJECTED') { %>
                  <div class="mt-3 text-warning small">
                    Refund request rejected.
                    <% if (o.refund.reject_reason) { %>
                      Reason: <%= o.refund.reject_reason %>
                    <% } %>
                  </div>
                <% } %>
              </div>
            </div>
          <% }) %>
        </div>
      <% } %>
    </div>
  </main>
  <!-- Refund notice modal (pre-refund) -->
  <div class="modal fade" id="refundNoticeModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
      <div class="modal-content card-frosted">
        <div class="modal-header border-0">
          <h5 class="modal-title fw-semibold">Before You Request a Refund</h5>
          <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body">
          <p class="mb-3">
            Refunds are credited to your <strong>PokeVault Pay</strong> wallet once approved.
          </p>
          <div class="form-check">
            <input class="form-check-input" type="checkbox" id="refundNoticeAgree">
            <label class="form-check-label" for="refundNoticeAgree">
              I understand and agree to receive the refund in PokeVault Pay.
            </label>
          </div>
        </div>
        <div class="modal-footer border-0">
          <button type="button" class="btn btn-outline-light" data-bs-dismiss="modal">Cancel</button>
          <button type="button" class="btn btn-cta" id="refundNoticeProceed" disabled>Proceed</button>
        </div>
      </div>
    </div>
  </div>
  <script>
    (function () {
      const modalEl = document.getElementById('refundNoticeModal');
      const agreeCheck = document.getElementById('refundNoticeAgree');
      const proceedBtn = document.getElementById('refundNoticeProceed');
      const openButtons = document.querySelectorAll('.refund-open-btn');
      if (!modalEl || !agreeCheck || !proceedBtn || !openButtons.length) return;

      const modal = new bootstrap.Modal(modalEl);
      let targetUrl = '';

      openButtons.forEach(btn => {
        btn.addEventListener('click', () => {
          targetUrl = btn.getAttribute('data-refund-url') || '';
          agreeCheck.checked = false;
          proceedBtn.disabled = true;
          modal.show();
        });
      });

      agreeCheck.addEventListener('change', () => {
        proceedBtn.disabled = !agreeCheck.checked;
      });

      proceedBtn.addEventListener('click', () => {
        if (!agreeCheck.checked || !targetUrl) return;
        window.location.href = targetUrl;
      });
    })();
  </script>
</body>
</html>
